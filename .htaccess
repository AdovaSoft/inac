#Server error
ServerSignature Off
#ServerTokens Prod


#Server Environment
<IfModule mod_env.c>
SetEnv HOST localhost
SetEnv USER root
SetEnv PASS
SetEnv DBASE websqflt_nas
</IfModule>
        #$all_due[$i] = $qur->party_adv_due($party[$i][0]);
        #echo  "<td>" . esc($party[$i][0]) . "</td>";